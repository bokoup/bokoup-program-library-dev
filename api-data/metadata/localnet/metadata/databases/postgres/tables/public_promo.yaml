table:
  name: promo
  schema: public
object_relationships:
  - name: campaignObject
    using:
      manual_configuration:
        column_mapping:
          campaign: id
        insertion_order: null
        remote_table:
          name: campaign
          schema: public
  - name: metadataObject
    using:
      manual_configuration:
        column_mapping:
          metadata: id
        insertion_order: null
        remote_table:
          name: metadata
          schema: public
  - name: mintObject
    using:
      manual_configuration:
        column_mapping:
          mint: id
        insertion_order: null
        remote_table:
          name: mint
          schema: public
select_permissions:
  - role: public
    permission:
      columns:
        - slot
        - write_version
        - active
        - burn_count
        - max_burn
        - max_mint
        - mint_count
        - campaign
        - id
        - metadata
        - mint
        - created_at
        - modified_at
      filter: {}
